# Terraform
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.backup
*.tfplan
terraform.tfvars

# PKI offline root CA artefacts (private keys must never be committed)
**/.pki/

# Local
.tmp/
.plans/
.idea/
